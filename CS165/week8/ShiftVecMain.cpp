#include <iostream>
#include <vector>
#include <string>
#include <iostream>
#include "ShiftVec.hpp"
using namespace std;


int main()
{
   // vector<int> myVec;
   // myVec.push_back(1);
   // myVec.push_back(2);
   // myVec.push_back(3);
   // myVec.push_back(4);
   // myVec.push_back(5);

   // ShiftVec<int> sv(myVec);

   // sv.shiftLeft(12);
   // sv.shiftRight(12);

   // vector<int> result = sv.getVec();

   // for(int i = 0; i<result.size(); i++){
       // cout << result[i] << "  ";
   // }

   // return 0;
}
